server {
        location <%= node['stackdriver']['plugins']['nginx']['status_endpoint_path'] %> {
                stub_status on;
                access_log   off;
                allow 127.0.0.1;
                deny all;
        }
}
